import { AvatarRenderPipe } from './avatar-render.pipe';

describe('AvatarRenderPipe', () => {
  it('create an instance', () => {
    const pipe = new AvatarRenderPipe();
    expect(pipe).toBeTruthy();
  });
});
